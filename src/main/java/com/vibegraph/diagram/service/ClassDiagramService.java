package com.vibegraph.diagram.service;

/**
 * Class diagram generator.
 *
 * Includes:
 * - Classes with fields and methods (visibility +, -, #)
 * - Inheritance (extends), implementation (implements)
 * - Associations (field type references)
 * - Dependencies (@Autowired)
 *
 * TODO: generateClassDiagram(projectId, packageFilter) → Mermaid syntax
 */
public interface ClassDiagramService {
    // TODO: Define methods
}
