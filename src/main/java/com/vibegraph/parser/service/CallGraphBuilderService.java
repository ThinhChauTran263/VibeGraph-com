package com.vibegraph.parser.service;

/**
 * Call graph builder.
 * Walks AST to find method invocations and builds CALLS edges.
 *
 * TODO:
 * - buildCallGraph(CompilationUnit) → List<CallEdge>
 * - resolveCallChain(method, depth) → execution flow
 */
public interface CallGraphBuilderService {
    // TODO: Define methods
}
