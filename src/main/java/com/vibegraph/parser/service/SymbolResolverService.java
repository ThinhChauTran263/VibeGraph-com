package com.vibegraph.parser.service;

/**
 * JavaParser Symbol Solver wrapper.
 * Resolves method calls, type references, inheritance.
 *
 * TODO:
 * - resolveMethodCall(MethodCallExpr) → resolved method
 * - resolveType(NameExpr) → resolved type
 * - configureTypeSolver(projectPath, jdkPath, jarPaths)
 */
public interface SymbolResolverService {
    // TODO: Define methods
}
