package com.vibegraph.parser.service;

/**
 * Java source code parser service.
 * Orchestrates JavaParser to extract nodes/edges from .java files.
 *
 * TODO:
 * - parseFile(filePath) → ParseResult
 * - parseProject(projectPath) → batch parse
 * - parseFileWithCache(filePath) → use SHA-256 cache
 */
public interface ParserService {
    // TODO: Define methods
}
