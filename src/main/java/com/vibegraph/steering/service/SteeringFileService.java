package com.vibegraph.steering.service;

/**
 * Steering file generator service.
 * Generates AI tool config files (Kiro, Cursor, Claude) from current architecture.
 *
 * TODO:
 * - generateAll(projectId)
 * - generateForKiro(projectId)
 * - generateForCursor(projectId)
 * - generateForClaude(projectId)
 */
public interface SteeringFileService {
    // TODO: Define methods
}
