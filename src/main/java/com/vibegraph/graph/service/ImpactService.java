package com.vibegraph.graph.service;

/**
 * Impact analysis service.
 * Computes blast radius when modifying a node.
 *
 * TODO:
 * - getBlastRadius(projectId, nodeId, depth) → ImpactAnalysisResponse
 * - findCallers(methodId) → upstream
 * - findCallees(methodId) → downstream
 */
public interface ImpactService {
    // TODO: Define methods
}
