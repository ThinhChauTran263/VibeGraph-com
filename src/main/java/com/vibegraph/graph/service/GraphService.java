package com.vibegraph.graph.service;

/**
 * Graph data service.
 *
 * TODO:
 * - getFullGraph(projectId) → GraphDataResponse
 * - getNodeDetail(projectId, nodeId) → NodeDetailResponse (INCOMING + OUTGOING)
 * - searchNodes(projectId, query) → matching nodes
 * - filterNodes(projectId, filter) → filtered subset
 */
public interface GraphService {
    // TODO: Define methods
}
